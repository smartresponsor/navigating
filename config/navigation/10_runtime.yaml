# Runtime roles, scopes, and environment metadata.
navigation:
  runtime_roles:
    fallback_roles:
    - ROLE_USER
  runtime_scopes:
    fallback_scopes:
    - user
    - system
  runtime_environment:
    fallback_environment: dev
